const assert = require('chai').assert;
const expect = require('chai').expect;
const dut = require('../src/functions.js');


describe('fuzzy search', function() {
    it('should find', function() {
        assert.notEqual(undefined, undefined);
    });
});